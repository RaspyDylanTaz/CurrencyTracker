#This app is a simple currency tracker which live tracks the top 10 crypto currencies against the NZD.

#By using the crypto compare API prices are fetched every three seconds and displayed in a simple format.

#To start the project run npm start



